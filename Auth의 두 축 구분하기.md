### 1. 인증 vs 인가 핵심 비교

|**구분**|**인증 (Authentication)**|**인가 (Authorization)**|
|---|---|---|
|**질문**|**"너 누구니?"** (Who are you?)|**"너 이거 할 수 있니?"** (What can you do?)|
|**목적**|사용자의 **신원(Identity)**을 증명/확인|인증된 주체에게 특정 리소스 접근 **권한(Permission)**을 부여|
|**대표 예시**|ID/비밀번호 입력, 생체 인식(Fingerprint, FaceID), 2FA 번호 입력|관리자 페이지 접근 권한, 파일 읽기/쓰기 권한, 구글 캘린더 수정 권한|
|**일상 비유**|**신분증 / 여권** (내가 누구인지 증명)|**공연장 티켓 / 놀이공원 자유이용권** (어디까지 입장 가능한지 증명)|

### 2. OAuth는 왜 '인가(Authorization)' 프로토콜일까?

이름부터 **Open Authorization**인 만큼, OAuth의 탄생 목적 자체가 "내가 가진 권한의 일부를 제3자(App)에게 빌려주기 위함"이기 때문입니다.

#### 💡 출입증 비유로 이해하기

- **인증 (Authentication):** 호텔 데스크에서 여권을 보여주고 **내가 본인임을 확인**받는 과정.
    
- **인가 (Authorization):** 데스크에서 **301호 문을 열 수 있는 스마트키 카드**를 받아 들어가는 과정.
    

OAuth는 "301호 문을 열 수 있는 키 카드(Access Token)"를 발급해 주는 표준 절차입니다.

스마트키 카드는 301호 문을 열 권한(인가)을 줄 뿐, **그 카드를 쥔 사람의 이름이나 생년월일(인증)을 직접 담고 있지 않는 것**과 같습니다.

### 3. 현실에서 겪는 가장 큰 혼란: "소셜 로그인"

실무에서 "구글로 로그인하기", "카카오 로그인"을 구현할 때 OAuth 2.0을 사용하다 보니, 많은 사람들이 "OAuth = 로그인(인증) 기술"이라고 오해합니다.

- **OAuth 자체만 쓸 때의 한계 (Pseudo-Authentication)**
    
    - OAuth 2.0이 내려주는 `Access Token`은 원래 "이 토큰으로 구글 캘린더 조회가 가능하다"는 권한 증명서일 뿐, 사용자 정보(이름, 이메일 등)를 보장하는 인증 표준이 아닙니다.
        
- **해결책: OIDC (OpenID Connect)**
    
    - OAuth 2.0의 **인가** 레이어 위에, 사용자 신원 정보(인증)를 담은 `ID Token`을 내려주도록 한 겹 더 얹은 표준이 바로 **OIDC**입니다.
        
    - 즉, 우리가 흔히 쓰는 '소셜 로그인'은 정확히 말하면 OAuth 2.0(인가) + OIDC(인증)의 결합입니다.
        

### 4. 정리

- **인증:** 당신이 유효한 사용자임을 증명하는 것
    
- **인가:** 인증된 사용자가 특정 자원에 접근하도록 허락하는 것
    
- **OAuth 2.0:** **Access Token**을 통해 리소스 접근 권한을 관리하는 **인가(Authorization) 전용 표준 프로토콜**