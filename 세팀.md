TwinCAT은 단순한 소프트웨어가 아니라 Windows 백그라운드에서 돌아가는 가상 PLC(OS 레벨 런타임)와 개발 환경(IDE)이 분리되어 통신하는 구조입니다. 이 두 계층(시스템 레벨 vs PLC 프로그램 레벨)을 나누어 이해하면 개념이 명확해집니다.

### 1. 시스템 레벨 (OS 및 하드웨어 런타임 제어)

PLC 엔진 본체를 켜고 끄거나 통신 모드를 바꾸는 기능입니다. 윈도우 우측 하단 트레이의 신호등 색상과 직결됩니다.

- **Config Mode (파란색 신호등 아이콘)**
    
    - **정의:** 시스템을 **설정(Configuration) 모드**로 전환합니다. (신호등: 파란색)
        
    - **용도:** 실제 PLC 로직은 실행되지 않으며, EtherCAT 네트워크 스캔, 하드웨어 I/O 통신 점검, 서보 드라이브 튜닝 등을 할 때 사용합니다.
        
- **Activate Configuration (파란색 박스 안 톱니바퀴 아이콘)**
    
    - **정의:** 현재 IDE에서 설정한 하드웨어 트리, I/O 매핑, 태스크 주기, 라이선스 정보 전체를 런타임 엔진에 영구 배포(Flash Write 성격)하고 런타임을 재부팅합니다.
        
    - **용도:** I/O 변수 링크를 새로 걸었거나, 슬레이브 장치를 추가/제거했을 때 반드시 거쳐야 하는 필수 단계입니다.
        
- **Restart TwinCAT System (툴팁에 표시된 회전 화살표 톱니바퀴 아이콘)**
    
    - **정의:** 설정을 새로 빌드하여 배포하지 않고, 현재 시스템에 이미 올라가 있는 기존 구성을 그대로 유지한 채 TwinCAT 런타임만 재시작(Run Mode 진입)시킵니다.
        
    - **용도:** 하드웨어 구성 변경 없이 단순히 멈춘 런타임을 다시 가동하거나 리셋할 때 사용합니다.
        

### 2. 프로젝트 및 프로그램 레벨 (PLC 코드 제어)

런타임 엔진이 켜진 상태(초록색 Run 모드)에서 사용자가 짠 프로그램 코드를 컴파일하고 주입하는 기능입니다.

- **Build Solution (또는 Build Project)**
    
    - **정의:** C/C++나 일반 소프트웨어의 빌드와 동일합니다.
        
    - **용도:** ST 코드나 래더 다이어그램의 문법 오류, 변수 선언 누락, 데이터 타입 불일치를 검사하여 실행 가능한 머신 코드로 번역합니다. 런타임에 전송하지 않고 PC 내부에서 컴파일만 수행합니다.
        
- **Login (초록색 문으로 들어가는 화살표 아이콘)**
    
    - **정의:** 컴파일된 코드를 실행 중인 TwinCAT 런타임에 다운로드하고, 개발 툴과 런타임 간 통신 채널을 열어 **실시간 모니터링 세션에 접속**합니다.
        
    - **용도:** 접점의 On/Off 상태를 보거나 변수 값을 확인할 때 누릅니다. 코드가 변경된 상태에서 누르면 "Online Change"를 할 것인지 묻습니다.
        
- **Logout (빨간색 문으로 나가는 화살표 아이콘)**
    
    - **정의:** 개발 환경의 **모니터링 세션만 종료**합니다.
        
    - **용도:** PLC 런타임과 설비 동작은 멈추지 않고 계속 돌아가며, 에디터의 텍스트 잠금을 풀고 코드를 오프라인에서 자유롭게 편집할 수 있는 상태로 빠져나옵니다.
        

### 핵심 워크플로 한눈에 보기

| **작업 상황**                | **실행 순서**                                                                                            |
| ------------------------ | ---------------------------------------------------------------------------------------------------- |
| **I/O 링크나 하드웨어를 건드렸을 때** | `Activate Configuration` $\rightarrow$ `Run Mode 전환(OK)` $\rightarrow$ `Login` $\rightarrow$ `Start` |
| **순수 코드(ST/래더)만 수정할 때**  | `Logout` $\rightarrow$ 코드 편집 $\rightarrow$ `Login` (`Login with online change` 선택)                   |
| **단순 문법 체크만 해보고 싶을 때**   | `Build Solution`                                                                                     |