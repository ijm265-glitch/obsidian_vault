### 1단계: 삼각함수 식에 오일러 공식 대입하기

삼각함수 형태의 $n$번째 조화 성분 식은 다음과 같습니다.

$$a_n \cos(n\omega_0 t) + b_n \sin(n\omega_0 t)$$

여기에 오일러 공식에서 유도한 $\cos\theta = \frac{e^{j\theta} + e^{-j\theta}}{2}$ 와 $\sin\theta = \frac{e^{j\theta} - e^{-j\theta}}{2j} = -j\frac{e^{j\theta} - e^{-j\theta}}{2}$ 를 그대로 대입합니다. ($\theta = n\omega_0 t$)

$$= a_n \left( \frac{e^{j\theta} + e^{-j\theta}}{2} \right) + b_n \left( \frac{e^{j\theta} - e^{-j\theta}}{2j} \right)$$

### 2단계: $e^{j\theta}$ (양의 주파수)와 $e^{-j\theta}$ (음의 주파수)로 묶기

분모의 $j$를 분자로 올리기 위해 $\frac{1}{j} = -j$를 이용해 묶어주고, 반시계 방향 회전 성분($e^{j\theta}$)과 시계 방향 회전 성분($e^{-j\theta}$)으로 식을 재정렬합니다.

$$= \left( \frac{a_n - j b_n}{2} \right) e^{j n \omega_0 t} + \left( \frac{a_n + j b_n}{2} \right) e^{-j n \omega_0 t}$$

### 3단계: 복소 계수 $c_n$ 정의하기

이 정리된 식의 계수를 유심히 살펴보면, 두 계수가 서로 **복소켤레(Complex Conjugate)** 관계라는 것을 알 수 있습니다.

이제 이 덩어리들을 $c_n$이라는 하나의 기호로 정의합니다.

1. **양의 주파수 성분 ($n > 0$):**
    
    $$c_n = \frac{a_n - j b_n}{2}$$
    
2. **음의 주파수 성분 ($n < 0$):**
    
    $$c_{-n} = \frac{a_n + j b_n}{2} = \overline{c_n}$$
    
3. **직류(DC) 성분 ($n = 0$):**
    
    $$c_0 = a_0$$
    

이렇게 정의하고 나면, 길었던 삼각함수 식 전체가 **단 하나의 깔끔한 복소 지수 합**으로 통일됩니다.

$$f(t) = a_0 + \sum_{n=1}^{\infty} \left( a_n \cos(n\omega_0 t) + b_n \sin(n\omega_0 t) \right) = \sum_{n=-\infty}^{\infty} c_n e^{j n \omega_0 t}$$