#core 
### 1. C# 프로젝트의 계층 구조

- **솔루션 (.sln)**: 여러 프로젝트를 묶는 최상위 바인더 컨테이너입니다.
    
- **프로젝트 (.csproj)**: **실제 하나의 실행 파일(`.exe`) 또는 라이브러리(`.dll`)를 만들어내는 최소 빌드 단위입니다. 여기에 사용할 .NET 버전(`net8.0`)과 외부 패키지(`NModbus4`, `System.IO.Ports`) 정보가 기록됩니다.**
    
- **네임스페이스 (namespace)**: 수많은 클래스들의 이름 충돌을 방지하기 위한 성(Family name)이자 폴더 개념입니다.
    
- **클래스 (class)**: 관련 데이터(변수)와 기능(함수)을 묶어둔 프로그램의 기본 설계도입니다.
    
- **메서드 (method)**: 실제 명령문들이 순차적으로 실행되는 구체적인 작업 블록입니다.

## 모든 실행 코드는 반드시 클래스 내부의 메서드에 위치해있어야 함

`class` : Program의 `method` : Main에서 실행코드를 작성하여 수행

