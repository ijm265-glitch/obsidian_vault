


### 화면 전환 스위치

스위치를 눌러 다른 화면으로 전환
고정된 화면값을 사용하여 이동 가능

#### 숫자 입력기를 통한 화면전환

프로젝트 - 화면 설정 - 디바이스에 의한 화면 전환 사용
- 화면 전환 디바이스: 그 디바이스의 값이 바뀌면 그 값으로 화면을 전환한다.
- 현재 화면 디바이스: 그 디바이스에 현재 화면 값을 저장한다. (이전 화면 복귀에 필요)

화면 전환 스위치의 구성을 마쳤으면 그 화면을 우클릭 하여 모든 기본화면의 배경으로 설정을 눌러 모든 화면에 화면 전환 스위치를 구성할 수 있다.


### 특수 스위치
특수 기능의 스위치 역할을 함 (알람, 로깅, 멀티미디어 재생 등)
언어 전환 예시
문자열표에 여러 언어를 등록해두고 특수 스위치를 언어 전환으로 설정하여 다국어 프로그램을 구성할 수 있다.

### 다중 스위치
여러가지 동작 (언어 변경, 램프 동작, 값의 복사 등등)을 동시에 수행할 수 있는 스위치![[Pasted image 20260820151304.png]]