1. File 생성
2. Part Design에서 시작
3. 바디를 생성하지 않고 전체의 대략적인 스케치 생성 (MasterSketch로 사용)
4. 전체의 대략적 스케치가 생성되었으면 각각의 부품을 바디로 만들 차례임
5. 바디를 하나 생성하고 MasterSketch를 눌러서 해당 바디 내부에 `Sub-Shape Binder`생성
![[Pasted image 20260908144611.png]]
- `Sub-Shape Binder`를 생성하는 이유는 MasterSketch가 만들고자 하는 부품의 외부에 존재하기 때문이며 `Binder`를 통해 참고할 수 있도록 만드는 것이다
6. `Sub-Shape Binder`의 `Make Face`를 `False`로 설정
7. `Binder`를 참고하여 그리고자 하는 부품을 다시 덧그리고` Pad Operation`등을 수행 


