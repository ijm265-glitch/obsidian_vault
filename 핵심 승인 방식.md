## 1. Authorization Code Grant (권한 부여 코드 승인)

사용자(Resource Owner)가 직접 로그인하고 권한을 부여하는 **가장 전형적이고 안전한 방식**입니다.

- **인증 주체:** 사용자 (User) + 클라이언트 애플리케이션
    
- **주요 사용처:** 백엔드 서버가 존재하는 일반적인 웹 서비스 (Confidential Client)
    
- **특징:**
    
    - 사용자가 Google, Kakao 로그인 창에서 직접 아이디/비밀번호를 입력합니다.
        
    - 서비스 서버는 **Access Token을 브라우저를 거치지 않고 인증 서버와 직접 통신하여 발급**받습니다.
        
    - 브라우저에는 전달 과정에서 `Authorization Code`만 노출되므로, 최종 Access Token이 중간에 탈취당할 위험이 매우 낮습니다.
        

> **참고:** 앞서 다룬 **Public Client(React, Mobile 등)** 환경에서는 이 방식에 **PKCE**를 결합하여 Client Secret 없이 안전하게 사용합니다.

## 2. Client Credentials Grant (클라이언트 자격 증명)

**사용자의 개입이 전혀 필요 없는 서버 대 서버(Server-to-Server) 통신**에 사용되는 방식입니다.

- **인증 주체:** 클라이언트 애플리케이션 자체 (서버)
    
- **주요 사용처:**
    
    - 서비스 내부의 Microservice(MSA) 간 통신
        
    - 사용자의 개인 데이터가 아니라, 시스템 전체 공통 데이터(예: 날씨 API, 공공 데이터, 배치 작업)를 조회할 때
        
- **특징:**
    
    - 로그인 화면이나 사용자 승인 단계가 아예 없습니다.
        
    - 서버가 보유한 **Client ID + Client Secret**만 가지고 인증 서버에 요청하여 Access Token을 즉시 발급받습니다.
        
    - Confidential Client(안전한 백엔드 서버)에서만 절대적으로 사용할 수 있습니다.