서보 드라이브나 스텝 드라이브에 고속 펄스열 또는 네트워크 출력을 발생시켜 액추에이터를 원하는 위치나 거리, 속도, 가감속 프로파일로 이동시키는 제어 모듈

### 왜 사용하는가?
- **PLC 스캔 타임의 물리적 한계:** 일반 PLC CPU의 래더 연산 주기(수 ms)와 일반 출력 카드의 응답 속도로는 초당 수백만 번 튀는 고속 펄스(최대 4Mpps)를 만들어낼 수 없습니다.
    
- **충격 없는 고속 가감속 프로파일 생성:** 정지 상태에서 최고 속도까지 모터를 급격히 돌리면 기계적 슬립과 탈조, 감속기 파손이 일어납니다. 부드러운 가속과 감속(사다리꼴, S자 곡선) 연산을 전용 하드웨어 칩(ASIC)이 실시간으로 전담 처리해 줍니다.
    
- **정밀 단위 변환 및 티칭 편의성:** 엔지니어가 펄스 개수를 일일이 세지 않고도 $\text{mm}$, $\text{deg}$, $\text{inch}$ 단위로 목표값을 설정하면 모듈이 볼스크루 리드값과 감속비에 맞춰 지령을 자동 변환합니다.

**어떤 방식으로 작동하는가?**

1. **파라미터 및 운전 데이터 설정:**
    
    - `XG-PM` 툴이나 래더 프로그램을 통해 축당 최대 400개의 운전 스텝(목표 위치, 운전 속도, 가속/감속 시간, 패턴 등)을 모듈의 비휘발성 메모리(FRAM)에 미리 등록합니다.
        
2. **기동 지령 (PLC CPU $\rightarrow$ 모듈):**
    
    - 메인 래더에서 "1번 축, 5번 스텝 기동"이라는 단일 기동 명령(4ms 내외)만 모듈에 전달합니다.
        
3. **궤적 생성 및 지령 출력 (모듈 $\rightarrow$ 드라이브):**
    
    - 모듈 내 전용 ASIC 칩이 설정된 가감속 곡선에 맞춰 실시간 주파수를 변조하며 펄스를 쏟아냅니다.
        
    - **오픈 컬렉터(XGF-POxH):** 범용 트랜지스터 펄스 출력 (최대 500kpps, 전송 거리 5m).
        
    - **라인 드라이버(XGF-PDxH):** 차동 신호 방식(RS-422A)으로 노이즈에 강함 (최대 4Mpps, 전송 거리 10m).
        
    - **네트워크형(XGF-PNxx):** 펄스선 대신 EtherCAT 통신으로 드라이브에 디지털 위치 지령 전달.
        
4. **완료 피드백:**
    
    - 설정한 펄스를 모두 출력하고 서보 드라이브로부터 위치 결정 완료 신호(In-Position)를 받으면 PLC CPU에 운전 완료 플래그를 올려줍니다.
        

**사양표로 보는 주요 기능 (뭘 할 수 있는가?)**

- **다양한 위치 제어 방식:**
    
    - **절대좌표(Absolute):** 기계 원점을 기준으로 한 절대 위치로 이동.
        
    - **상대좌표(Incremental):** 현재 서 있는 위치를 기준으로 지정한 변위만큼 증분 이동.
        
    - **조그(JOG) 및 MPG(수동 펄스 발생기) 운전:** 티칭 작업 시 작업자가 조그 버튼이나 핸들 휠로 모터를 미세 수동 이송.
        
- **운전 모드 전환 제어:**
    
    - **속도/위치 전환:** 컨베이어처럼 일정한 속도로 계속 회전하다가, 제품 감지 센서 신호(외부 기동 신호)가 들어오는 즉시 정밀 감속하여 지정 위치에 안착(피더/정촌 공급).
        
    - **위치/토크 전환:** 정해진 위치까지 빠르게 진입한 후, 부품 체결/압입 단계에서 모터 토크를 제한하며 누르는 압입 작업.
        
- **기본 보간 및 다축 협조 운전 (2~4축):**
    
    - 모션 전용 CPU보다는 단순하지만, 2개 이상의 축을 동시에 움직여 사선 이동(**직선 보간**), 2축 평면 원 호 그리기(**원호/타원 보간**), 3축 나선 이동(**헬리컬 보간**)을 지원합니다.
        
- **FRAM 메모리 적용:**
    
    - 배터리 없이도 운전 데이터와 파라미터가 영구 보존되며, 플래시 메모리와 달리 쓰기 횟수 수명 제한 없이 현장에서 실시간 좌표 티칭(Teaching) 값을 고속 저장할 수 있습니다.