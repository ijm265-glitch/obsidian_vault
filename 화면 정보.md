![[Pasted image 20260912202630.png]]
Target System : 작성한 PLC 프로그램과 하드웨어 구성을 실제로 다운로드해서 실시간 제어를 실행할 '목적지 제어기(실행 엔진)
프로그램을 작성하고 IPC에 쓰기작업을 수행하기 위해 사용


![[Pasted image 20260912203936.png]]

- **SYSTEM (시스템 및 하드웨어 구성)**
    - PC의 CPU 코어를 실시간(Real-time) 제어용으로 할당하고, 베이스 제어 주기(Base Time, 예: 1ms) 및 태스크 우선순위를 지정합니다.
- **MOTION (모션 제어 / NC 및 CNC)**
    - 서보 모터, 스텝 모터, 인버터 축을 등록하고 제어 파라미터(단위당 펄스 수, 가감속도, 리미트 센서)를 설정합니다
        
- **PLC (프로그래밍 가능 로직 제어)**
    - 설비의 시퀀스 제어 로직을 작성하는 핵심 영역입니다.
    - IEC 61131-3 표준 언어(ST, Ladder, SFC 등)로 코드(POU)를 작성하고, I/O 변수 및 전역 변수(GVL)를 선언합니다.
        
- **SAFETY (안전 제어 / TwinSAFE)**
    - 비상정지(E-Stop), 안전 라이트 커튼, 도어 인터록 등 작업자 안전 관련 규격(SIL3 / Cat.4)을 충족하는 전용 안전 로직을 별도로 분리해 프로그래밍하는 곳입니다.
        
- **C++ (실시간 C/C++ 모듈)**
    - PLC 언어(ST) 대신 C++로 실시간 제어 알고리즘을 직접 짜고 싶을 때 사용합니다.
    - 복잡한 수학 수식 모델, 이미지 처리 필터, 물리 해석 수치 모델 등을 고속으로 돌릴 때 주로 쓰입니다.
        
- **VISION (TwinCAT Vision)**
    - 공장 카메라(GigE Vision 등)로부터 영상을 캡처해 결함 검사, 바코드/QR 인식, 치수 측정 등의 머신비전 알고리즘을 PLC 실시간 커널 안에서 직접 처리하는 모듈입니다.
        
- **ANALYTICS (데이터 분석 및 모니터링)**
    - 설비 가동 중 발생하는 센서 신호, 사이클 타임, 진동 데이터 등을 실시간으로 수집·로깅하여 이상 징후를 예측하고 설비 효율(OEE)을 분석하는 도구입니다.
        
- **[[I-O]] (물리적 입출력 / 필드버스)**
    - PC 랜카드에 꽂힌 **EtherCAT 네트워크**를 스캔하거나, 리모트 I/O 슬레이브(디지털 입출력 커플러, 아날로그 센서 모듈, 서보 드라이브 등)를 등록하는 곳입니다.
    - 여기서 찾은 하드웨어 단자 핀과 `PLC` 모듈의 프로그래밍 변수(`%I*`, `%Q*`)를 드래그 앤 드롭으로 1:1 매핑(Link)합니다.
