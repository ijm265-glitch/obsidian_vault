와일드카드 카드(`*`)를 사용해 패턴에 맞는 파일 경로들을 한 번에 검색

## 주요 와일드카드 패턴

| **패턴**      | **의미**                    | **사용 예시**        | **설명**                         |
| ----------- | ------------------------- | ---------------- | ------------------------------ |
| **`*`**     | 임의의 길이를 가진 모든 문자열 매칭      | `*.jpg`          | 확장자가 `.jpg`인 모든 파일             |
| **`**`**    | 현재 폴더 및 모든 하위 폴더 (재귀적 탐색) | `**/*.png`       | 하위 폴더 전체에서 `.png` 파일 검색        |
| **`?`**     | 딱 1개의 임의의 문자 매칭           | `img_?.png`      | `img_1.png`, `img_a.png` 등 매칭  |
| **`[0-9]`** | 대괄호 안의 범위/문자 중 1개 매칭      | `file_[0-9].txt` | `file_0.txt` ~ `file_9.txt` 매칭 |

## 핵심 함수
`glob.glob(pathname, recursive=True) -> list[str]`
`glob.iglob(pathname, recursive=True) -> Iterator[str]`



