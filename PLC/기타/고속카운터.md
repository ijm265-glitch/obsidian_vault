고속카운터 모듈(High-Speed Counter Module, HSC)은 일반 디지털 입력 카드가 감지할 수 없는 **수십~수백 kHz(초당 수십만 번) 이상의 초고속 전기 펄스를 누락 없이 계수(Counting)하고 위치·속도를 연산**하는 전용 하드웨어 모듈입니다.

**왜 사용하는가?**

- **PLC 스캔 타임의 물리적 한계:**
    
    - 일반 PLC의 래더 연산 주기(스캔 타임)는 약 $1 \sim 10,\text{ms}$입니다. 이 주기로는 $1\,\text{kHz}$(주기 $1\,\text{ms}$)를 넘는 펄스 신호를 소프트웨어(일반 카운터 명령)로 읽을 경우 신호를 놓치는 결함(누락)이 발생합니다.
        
- **센서의 초고속 펄스 출력:**
    
    - 서보 모터나 회전축에 장착된 로터리 엔코더는 1회전에 수백~수만 개의 펄스를 쏟아냅니다. 모터가 고속 회전하면 수백 kHz의 신호가 발생하므로, 메인 CPU와 별개로 **하드웨어 카운터 칩이 실시간으로 펄스를 주워 담아야** 합니다.
        
- **즉각적인 하드웨어 출력(일치 출력) 필요:**
    
    - "정확히 $5,000$번째 펄스가 들어오는 그 순간(마이크로초 단위)" 솔레노이드나 카메라 트리거를 켜야 할 때, PLC 래더의 스캔 딜레이를 기다리지 않고 모듈 자체 핀에서 즉시 신호를 쏘아줘야 합니다.
        

**어떤 방식으로 작동하는가?**

1. **하드웨어 입력 및 신호 판별:**
    
    - 엔코더로부터[[ A상, B상, Z상]] 신호를 입력받습니다.
        
    - **위상차 판별 (A/B상 90° 위상차):** A상이 먼저 들어오면 정회전(가산 카운트 $+1$), B상이 먼저 들어오면 역회전(감산 카운트 $-1$)으로 방향을 스스로 판별합니다.
        
    - **배수 연산 (1체배, 2체배, 4체배):** 펄스의 상승 에지와 하강 에지를 모두 계측하여 엔코더 분해능을 최대 4배까지 뻥튀기해 정밀도를 높입니다.
        
2. **독립 카운팅 및 연산:**
    
    - PLC CPU가 다른 래더 코드를 돌리고 있든 말든, 모듈 내부 카운터 레지스터에 현재 펄스 누적값이 실시간으로 갱신됩니다.
        
    - Z상(원점 펄스) 신호나 외부 리셋 단자 신호가 들어오면 카운터를 즉각 $0$으로 초기화합니다.
        
3. **일치 비교 및 인터럽트 출력:**
    
    - 설정해 둔 프리셋 값(비교값)과 현재 카운트 값이 일치하는 순간, 외부 출력 단자로 신호를 직접 내보내거나(일치 출력) PLC CPU에 고속 인터럽트를 발생시킵니다.
        

**현장에서 무엇을 할 수 있는가?**

- **컨베이어 제품 위치 추종 및 불량 선별 (트래킹):**
    
    - 컨베이어 롤러에 엔코더를 달아 고속카운터에 연결하면 컨베이어가 이동한 거리가 펄스로 환산됩니다. 상류 비전 검사기에서 불량을 감지한 뒤, 정확히 컨베이어가 $1,250\,\text{mm}$ 이동했을 때 에어 블로우(실린더)를 쳐서 불량품을 쳐내는 작업을 딜레이 없이 구현합니다.
        
- **회전축 RPM 및 선속도 측정:**
    
    - 단위 시간(예: $10\,\text{ms}$ 또는 $100\,\text{ms}$) 동안 들어온 펄스 수를 측정하여 롤러나 모터의 회전 속도(RPM)를 정밀 계산합니다.
        
- **비전 검사기 고속 트리거 (카메라 촬영 신호):**
    
    - 제품이 지나가는 동안 정해진 간격(예: 매 $10\,\text{mm}$ 펄스마다)으로 머신비전 카메라에 고속 하드웨어 트리거 펄스를 쏴서 모션 블러 없이 연속 촬영을 수행합니다.
        
- **수동 조작 핸들(MPG, 수동 펄스 발생기) 입력:**
    
    - 반도체/공작기계 장비 세팅 시 작업자가 손으로 돌리는 수동 펄스 핸들의 회전량을 고속카운터로 받아 서보 축을 미세 이송(Jog/Inching)시킵니다.