## 1. 흐름의 전체 4단계
- 어떤 사용자가 어떤 사이트에서 `구글로 로그인하기`를 누르는 상황
#### ① 사용자(리소스 오너)는 로그인을 수행.
- 사용자가 클라이언트 앱에서 구글 로그인 버튼을 누릅니다.
- 화면이 구글 로그인창(인증 서버)으로 바뀝니다. 사용자는 구글 아이디/비밀번호를 치고 로그인 및 권한 동의("이 앱이 네 프로필 읽는 거 허용할래?")를 마칩니다.
- **사용자가 하는 일은 여기서 끝입니다!** 사용자는 토큰이나 코드를 직접 받아오지 않습니다.
#### ② 인증 서버(구글)가 '임시 코드(Authorization Code)'를 발급합니다.
- 사용자가 로그인을 성공하면, 인증 서버는 사용자의 브라우저를 클라이언트 앱의 주소(Redirect URI)로 돌려보냅니다(리다이렉트).
- 이때 주소 뒤에 **`code=abc1234`** 형태로 **임시 코드**를 실어서 보냅니다.
- 즉, **코드는 인증 서버(구글)가 만들어서 클라이언트(우리 앱)에게 전달**합니다.

#### ③ 클라이언트(우리 앱)가 '토큰'으로 교환합니다.
- 클라이언트의 백엔드 서버는 방금 전달받은 `code=abc1234`를 낚아채서, 구글 인증 서버로 비밀리에 직접 연결(서버 대 서버 통신)합니다.
- *"구글님, 방금 브라우저 통해서 전달받은 코드(`abc1234`)랑 저희 앱 **비밀키(`Client Secret`)** 드릴테니, 진짜 `Access Token`*_으로 바꿔주세요."_
- 인증 서버가 검증 후 클라이언트 백엔드에게 `Access Token`을 쥐여줍니다.
#### ④ 클라이언트가 리소스 서버(API) 데이터를 가져옵니다.
- 이제 클라이언트는 이 `Access Token`을 가지고 리소스 서버(구글 캘린더, 카카오 프로필 등)에 요청을 보내 사용자의 데이터를 가져옵니다.

## 1. CSRF (Cross-Site Request Forgery)와 OAuth `state` 파라미터

**CSRF**는 사용자가 로그인된 상태를 악용하여, **사용자의 의도와 상관없이 공격자가 원하는 요청(비밀번호 변경, 송금 등)을 서버로 전송**하게 만드는 공격입니다.

### OAuth에서 CSRF가 발생하는 시나리오

OAuth 인증 과정에서 공격자가 자신의 계정으로 인증을 진행하다가, 인증 서버로부터 발급받은 `Authorization Code`를 탈취하거나 직접 확보합니다. 이후 피해자가 공격자의 `Authorization Code`가 포함된 리다이렉트 URL을 클릭하도록 유도합니다.

피해자의 브라우저가 이 코드를 서비스 서버로 보내면, **피해자의 서비스 계정에 공격자의 소셜 계정(예: Google, Kakao)이 연동**되는 심각한 보안 사고가 발생합니다.

### `state` 파라미터의 역할

`state` 파라미터는 **CSRF 공격을 방지하기 위한 난수(CSRF 토큰)** 역할을 합니다.

1. 클라이언트(웹 앱)는 인증 요청을 보낼 때 추측할 수 없는 임의의 값(`state`)을 생성하여 세션/쿠키에 저장하고, 인증 서버로 함께 보냅니다.
    
2. **인증 서버**는 인증 완료 후 리다이렉트할 때 이 `state` 값을 그대로 돌려줍니다.
    
3. **클라이언트**는 돌아온 `state` 값과 자신이 처음에 저장해 둔 `state` 값을 비교합니다.
    
4. 두 값이 일치해야만 "내가 요청한 인증의 응답이 맞다"고 판단하고 다음 단계를 진행합니다.