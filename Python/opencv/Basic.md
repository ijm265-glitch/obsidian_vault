```python
while cap.is_Ppened()
	...
	# q를 눌러서 정지
    if cv2.waitKey(10) & 0xFF == ord('q'):
        break
```
- cv2.waitKey 함수는 정해진 시간동안 대기하면서 키보드 입력을 받음
- 이 때 키보드 입력에 대해 32비트 정수를 반환한다. 
- 0xFF는 하위 8비트가 전부 1이고 나머지는 0인 32비트가 됨
- 따라서 키보드 입력과 비트연산을 통해 올바른 비트 값을 추출할 수 있다.