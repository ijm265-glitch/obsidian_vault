- 클라이언트와 API 서버의 중간에 끼어있는 소프트웨어
- 클라이언트의 request가 API 함수의 인자로 들어오기 전 미들웨어를 거침
- 서버의 response가 전송되기 전 미들웨어를 거침
`왜 쓰나요?`
- 모든 API 함수마다 공통되는 "시간 측정", "로그 남기기"등의 작업을 서버에 들어오고 나가는 모든 요청과 응답을 일괄적으로 통제, 조작
```python

```